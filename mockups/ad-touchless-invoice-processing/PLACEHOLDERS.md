# Placeholders — `/ad-touchless-invoice-processing`

Every `{{TOKEN}}` used in `index.html`, per 00-FOUNDATION.md §2 (claims discipline).

| Token | What's needed | Owner |
|---|---|---|
| `{{AD_HEADLINE_1..3}}`, `{{AD_DESCRIPTION_1}}` | The live Campaign 11 / EDUCATION ad group headlines this page must stay consistent with (QS requirement 3). See NOTES.md — this ad group's head term itself is an open question, so these can't be sourced until that's resolved. | Paid media / copy |
| `{{PAGE_LINK_3WAY_MATCHING}}` | Links to a "3-way matching" glossary concept that doesn't exist yet as a page. | Content / SEO |
| `{{PAGE_LINK_CAPTURE_METHODS}}` | Links to an "invoice capture methods" glossary concept that doesn't exist yet as a page. | Content / SEO |
| `{{YOOZ_BUSINESS_ADDRESS}}` | Footer legal address placeholder, required by QS requirement 4. | Legal / IT |

## Facts stated directly (not tokenized)

Per 00-FOUNDATION.md §2: "more than 250 financial systems," and the eight capture
channels (email, drag & drop, mobile, scan, sFTP, e-invoicing, fetcher, API).

No processing-time-reduction stat, touchless-rate percentage, customer names/logos,
G2 rating, or price benchmark appear on this page. A "touchless rate" percentage is
the single most tempting number to fabricate for this specific topic — it was
deliberately left out rather than guessed.
