# Placeholders — `/ad-ap-automation-manufacturing`

Every `{{TOKEN}}` used in `index.html`, per Foundation §2 claims discipline.

## Ad continuity (top-of-file comment)
- `{{AD_HEADLINE_1..3}}`, `{{AD_DESCRIPTION_1..2}}` — copy team: paste live
  RSA copy for Campaign 08 FIT – Industry, ad group Manufacturing.

## Vertical vocabulary — needs vertical SME sign-off before publish
These terms come from `09-fit-industry.md` itself (not invented for this
build), but are flagged for a manufacturing AP SME to confirm before
publish, per the brief's own claims-discipline note.
- `DOCTYPE_MANUFACTURING_1` — "goods receipts" (workflow section, list item 1)
- `DOCTYPE_MANUFACTURING_2` — "three-way match against production POs"
  (workflow section, list item 2)
- `APPROVAL_CHAIN_MANUFACTURING` — "plant and finance approval chain"
  (hero subhead + workflow section). No specific chain structure was
  given in the brief, so this stays generic pending SME input.

## Proof section
- `{{CUSTOMER_QUOTE_MANUFACTURING}}` — customer quote, needs a real
  customer and real permission to publish.
- `{{CUSTOMER_MANUFACTURING_1}}`, `{{CUSTOMER_MANUFACTURING_2}}` — named
  manufacturing customer/peer references.
- `{{CUSTOMER_LOGO_MANUFACTURING_1}}`, `{{CUSTOMER_LOGO_MANUFACTURING_2}}`
  — customer logo marks.
- `{{CASE_STUDY_MANUFACTURING}}` / `{{CASE_STUDY_MANUFACTURING_URL}}` —
  the secondary-conversion case study asset (title + link), referenced
  twice (proof section, form section).

## Footer
- `{{COMPANY_ADDRESS}}` — physical business address (QS transparency
  requirement, Foundation §3.4).

## Not tokenized — stated directly per Foundation §2
"Yooz exports to more than 250 financial systems" and the multi-channel
capture list (email, drag & drop, mobile, scan, sFTP, e-invoicing,
fetcher, API) are the two facts Foundation clears for direct use; both
appear in the Product section without a token.
