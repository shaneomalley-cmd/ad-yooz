# Placeholders — `/ad-accounts-payable-software`

Every `{{TOKEN}}` used in `index.html`, per Foundation §2 claims discipline. Nothing
below is a real number, name, or quote — all need sourcing before this page ships.

| Token | Where it's used | What's needed | Owner |
|---|---|---|---|
| `{{AD_HEADLINE_1}}` / `{{AD_HEADLINE_2}}` / `{{AD_HEADLINE_3}}` / `{{AD_DESCRIPTION_1}}` | Comment block, top of `<head>` | Actual RSA headline/description text from Campaign 03 (CATEGORY – Function-Named), all four ad groups, so above-fold copy can be checked for ad-to-page continuity | Paid media / copy team |
| `{{PRICE_PER_INVOICE_NATIVE}}` | Cost-per-invoice callout (Pattern Pink strip) + comparison table, "Cost per invoice" row | A citable average cost-per-invoice figure for processing through a native ERP/accounting-system AP module. Needs a named source (analyst firm or internal benchmark), same rigor as the existing calculator footnote | Analyst research / product marketing |
| `{{PRICE_PER_INVOICE_YOOZ}}` | Cost-per-invoice callout + comparison table | Yooz's own average cost-per-invoice figure, on a comparable basis to the native figure above (same volume tier, same cost components) | Product marketing |
| `{{CUSTOMER_MIGRATED_FROM_NATIVE_AP_QUOTE}}` | Proof section, quote card | A real, on-record quote from a named customer who specifically moved off an ERP's native AP module onto Yooz — must be sourced from customer marketing/case studies, not written speculatively | Customer marketing / case studies |
| `{{CUSTOMER_MIGRATED_NAME}}` | Proof section, quote attribution | That customer's real name, with permission to use | Customer marketing |
| `{{CUSTOMER_MIGRATED_TITLE}}` | Proof section, quote attribution | That customer's real job title | Customer marketing |
| `{{CUSTOMER_MIGRATED_COMPANY}}` | Proof section, quote attribution | That customer's real company name, with permission to use | Customer marketing |
| `{{CUSTOMER_MIGRATED_PRIOR_ERP_MODULE}}` | Proof section, migration tag | The specific ERP/native-module name that customer moved off (e.g. "Sage's native AP module") — must match what the customer actually said, not the ERP we'd like to argue against | Customer marketing |
| `{{G2_RATING}}` | Proof section, badge card | Current G2 star rating | Product marketing / review-site dashboards |
| `{{G2_REVIEW_COUNT}}` | Proof section, badge card | Current G2 review count | Product marketing / review-site dashboards |
| `{{COMPANY_PHYSICAL_ADDRESS}}` | Footer | Yooz's current registered/mailing address for the transparency/navigability requirement | Legal / IT |

## Verified facts used directly (not placeholders)

Per Foundation §2, these two are cited verbatim from the brand guidelines and do not
need a token:

- **250+ financial systems** — used in the "Why a dedicated layer wins" copy, the
  comparison table's multi-entity row, and the Export stage.
- **Multi-channel capture** (email, drag & drop, mobile, scan, sFTP, e-invoicing,
  fetcher, API) — used in the Capture stage of the six-stage process strip.

The six-stage vocabulary (Purchase → Capture → Review → Approve → Pay → Export) is
brand-approved per Foundation §2 and reused as-is, not invented for this page.
