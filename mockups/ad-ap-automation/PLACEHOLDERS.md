# Placeholders — `/ad-ap-automation`

Every `{{TOKEN}}` used in `index.html`, what it needs, and who should own filling it.

## Strategy decision (not a design one)

| Token | Needs | Owner |
|---|---|---|
| `{{PRIMARY_DIFFERENTIATOR_HEADLINE}}` | One differentiator clause for the H1's Pink half, on a natural clause boundary after "AP automation software". Candidates listed in an HTML comment above the `<head>` in `index.html`, all traceable to verified brand material: (a) proprietary deep-document-understanding AI, (b) 250+ financial system exports, (c) multi-channel capture breadth. Pick one — **not a blend of three.** | Marketing / positioning lead |
| `{{PRIMARY_DIFFERENTIATOR_META_CLAUSE}}` | Same decision, restated to fit the meta description's ≤155-char budget. | Marketing / positioning lead |
| `{{AD_HEADLINE_1}}` `{{AD_HEADLINE_2}}` `{{AD_HEADLINE_3}}` `{{AD_DESCRIPTION_1}}` `{{AD_DESCRIPTION_2}}` | The live Campaign 02 RSA headlines/descriptions, so the hero copy can be checked for ad-to-page continuity. `{{AD_HEADLINE_2}}` must match whichever differentiator is chosen above. | Paid media / copy team |

## Statistics — need re-substantiation or sourcing

| Token | Needs |
|---|---|
| `{{STAT_EXTRACTION_ACCURACY}}` | A citable field-level extraction accuracy figure for the AI capture claim in the capability grid. Do not ship a round number without a source. |

## Customers and quotes

| Token | Needs |
|---|---|
| `{{CUSTOMER_QUOTE_ENTERPRISE_1..3}}` | Real, permissioned customer quotes, sized to a similar company scale as the visitor (mid-market/enterprise). |
| `{{CUSTOMER_NAME_ENTERPRISE_1..3}}` | Real customer names, permissioned for use in paid media. |
| `{{CUSTOMER_TITLE_1..3}}` / `{{CUSTOMER_COMPANY_1..3}}` | Matching title/company for each quote above. |

## Logos

| Token | Needs |
|---|---|
| `{{ERP_LOGO_SAGE}}` `{{ERP_LOGO_NETSUITE}}` `{{ERP_LOGO_SAP}}` `{{ERP_LOGO_QUICKBOOKS}}` `{{ERP_LOGO_DYNAMICS}}` `{{ERP_LOGO_ACUMATICA}}` | Actual ERP/partner logo marks, cleared for use (some require a partner-program logo license, not just fair use). Currently rendered as dashed-border placeholder boxes per the imagery policy in `00-FOUNDATION.md`. |

## Legal / operational

| Token | Needs |
|---|---|
| `{{COMPANY_ADDRESS}}` | Yooz's current registered/physical business address for the footer, per QS transparency requirement 4. |

## Verified facts used directly (not tokens)

Per `00-FOUNDATION.md` §2, these two are stated directly in `index.html`, not tokenized:
- "250+ financial system exports" (ERP connector section, capability grid)
- Multi-channel capture list: email, drag & drop, mobile, scan, sFTP, e-invoicing, fetcher, API (step 1, capability grid)
